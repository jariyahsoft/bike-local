name: bike_local_admin_portal
publish_to: none
description: Admin portal shell for approval, moderation, support, reports, and audit workflows.
environment:
  sdk: ">=3.7.0 <4.0.0"
dependencies:
  flutter:
    sdk: flutter
